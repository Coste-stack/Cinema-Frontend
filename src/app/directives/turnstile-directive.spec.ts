import { TurnstileDirective } from './turnstile-directive';

describe('TurnstileDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnstileDirective();
    expect(directive).toBeTruthy();
  });
});
